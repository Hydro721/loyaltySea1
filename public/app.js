// Client-side JavaScript
document.addEventListener('DOMContentLoaded', () => {
    const app = document.getElementById('app');
    app.innerHTML = '<p>This is a single-page app using Node.js and Express!</p>';
});
